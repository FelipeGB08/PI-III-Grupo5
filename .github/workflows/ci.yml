name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  backend-unit:
    name: Backend Unit Tests
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ca_backend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: ca_backend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        run: npm test

  backend:
    name: Backend E2E
    runs-on: ubuntu-latest
    needs: backend-unit

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: conecta_amauc
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d conecta_amauc"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      NODE_ENV: test
      PORT: 3000
      ALLOWED_ORIGINS: http://localhost:3000,http://localhost:56000
      JWT_SECRET: ci_jwt_secret_apenas_para_testes
      DB_HOST: localhost
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: postgres
      DB_NAME: conecta_amauc
      GOOGLE_CLIENT_ID: ci-google-client
      APPLE_CLIENT_ID: ci-apple-client

    defaults:
      run:
        working-directory: ca_backend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: ca_backend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Syntax check
        run: |
          find src scripts -name "*.js" -print0 | xargs -0 -n1 node --check

      - name: Migrate database
        run: npm run db:migrate

      - name: Seed database
        run: npm run db:seed

      - name: Start API
        run: |
          npm run start > server.log 2>&1 &
          for i in {1..30}; do
            curl -fsS http://localhost:3000/api/status && exit 0
            sleep 1
          done
          cat server.log
          exit 1

      - name: Run E2E
        run: npm run test:e2e

  flutter:
    name: Flutter Analyze/Test
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ca_frontend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze

      - name: Test
        run: flutter test

  deploy-render:
    name: Deploy Render
    runs-on: ubuntu-latest
    needs:
      - backend
      - flutter
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    env:
      RENDER_DEPLOY_HOOK_URL: ${{ secrets.RENDER_DEPLOY_HOOK_URL }}

    steps:
      - name: Trigger Render deploy
        if: env.RENDER_DEPLOY_HOOK_URL != ''
        run: curl --fail-with-body --silent --show-error --request POST "$RENDER_DEPLOY_HOOK_URL"

      - name: Skip deploy without configured secret
        if: env.RENDER_DEPLOY_HOOK_URL == ''
        run: echo "Deploy ignorado; configure o secret RENDER_DEPLOY_HOOK_URL para habilita-lo."
